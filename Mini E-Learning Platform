import { BookOpen, GraduationCap } from "lucide-react";
import { CourseCard } from "@/components/CourseCard";
import { coursesData } from "@/data/courses";

const Index = () => {
  return (
    <div className="min-h-screen bg-background">
      {/* Hero Section */}
      <header className="bg-gradient-primary text-white">
        <div className="max-w-7xl mx-auto px-4 py-16 sm:px-6 lg:px-8">
          <div className="text-center space-y-6">
            <div className="flex justify-center">
              <GraduationCap className="w-16 h-16" />
            </div>
            <h1 className="text-4xl sm:text-5xl md:text-6xl font-bold tracking-tight">
              Welcome to LearnHub
            </h1>
            <p className="text-xl sm:text-2xl text-white/90 max-w-3xl mx-auto">
              Unlock your potential with our curated collection of courses. Start learning today and
              master new skills at your own pace.
            </p>
            <div className="flex items-center justify-center gap-8 text-sm sm:text-base">
              <div className="flex items-center gap-2">
                <BookOpen className="w-5 h-5" />
                <span>{coursesData.length} Courses</span>
              </div>
              <div className="flex items-center gap-2">
                <GraduationCap className="w-5 h-5" />
                <span>Expert Instructors</span>
              </div>
            </div>
          </div>
        </div>
      </header>

      {/* Courses Grid */}
      <main className="max-w-7xl mx-auto px-4 py-12 sm:px-6 lg:px-8">
        <div className="mb-8">
          <h2 className="text-3xl font-bold text-foreground mb-2">Available Courses</h2>
          <p className="text-muted-foreground">
            Choose from our selection of courses and start your learning journey
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {coursesData.map((course) => (
            <CourseCard key={course.id} course={course} />
          ))}
        </div>
      </main>

      {/* Footer */}
      <footer className="bg-card border-t border-border/50 mt-16">
        <div className="max-w-7xl mx-auto px-4 py-8 sm:px-6 lg:px-8">
          <p className="text-center text-muted-foreground">
            © 2025 LearnHub. Start your learning journey today.
          </p>
        </div>
      </footer>
    </div>
  );
};

export default Index;
